# 草稿纸大战 · 项目边界

这份文件是护栏，不是说明书。玩法和实现讲在 [README](README.md) 里。

写它的原因：这东西是"还原小时候在练习本边角画的战争"，但每一轮改进都在把它往
军事模拟器的方向推一点——三级装甲、武器按期望伤害选、启动时校准比例、配了无头
测试台。单看每一步都合理，累积起来就不是那个涂鸦了。所以把线画在这里。

## 这是什么

一张会自己打起来的涂鸦。你画部队上去，它们自己打；你再也不碰它，这仗照样打完。
看着像圆珠笔画在方格纸上。

**纯观赏 + 轻干预。** 玩家永远不直接指挥单位——不能选中一个坦克叫它去哪。你只能
画部队、点指令（空袭/炮击/增援这类），剩下的看着。这条是这个游戏的定义，破了它
就是另一个游戏了。

## 硬边界

1. **每个版本是一个自包含的 HTML。** 零依赖、零构建步骤、零网络请求，双击就能跑。
   `tools/` 是开发时的量尺，不是运行时的一部分——游戏本体永远不 import 它。
2. **不上框架、不拆模块、不引包管理器。** 有人提议"这该用 TypeScript 重构"时，
   答案是不。
3. **不加账号、存档、联网、排行榜、分析埋点。**
4. **用户的服务器信息不进仓库**（IP、端口、SSH 别名、部署脚本、nginx 路径）。
   这条是明确要求过的。
5. **画风统一。** 手绘抖动线条，同一支笔。新画法要经得起"和旁边的单位摆在一起
   看，是不是一支笔画的"。

## 软边界（默认不做，除非明确要求）

- **看不见的深度不要加。** 判据只有一个：这个改动能不能在画面上看出来？弹道下坠、
  燃料消耗曲线、装甲倾角——玩家看不出来，就别写。克制矩阵是踩着这条线过的：它本身
  看不见，所以兵种表的提示框必须把"◎血肉 ·薄皮 ·重甲"摊开给玩家看。看不见的规则
  要么删掉，要么想办法让它看得见。
- **每个弹种必须有短板。** `ARM` 里新开一行之前，先说得出这一行打什么不行。出现一个
  什么都能打的弹种，造哪种兵都一样，克制就不存在了——冷战版的 A-10 机炮就是被这条
  按住的：它扫步兵和轻车，反坦克交给同机的火箭。
- **型号考据只活在提示框里。** `Wfr.Gr.21`、`BGM-71 TOW` 这些查证得认真，但它们
  不能影响玩法，不看提示框的人不该被它们打扰。
- **新东西优先做成奇观，而不是常备兵种。** 兵种表已经十六七个了，再加就杂乱
  （这条是踩过的：加到二十个时用户说"单位太多了 杂乱"）。奇观偶尔出现、
  自带前置条件，加多少都不挤。
- **一局时长 8–12 分钟。** 当前中位约 11 分钟；一战版偶尔僵持到 18 分钟以上
  （六局里约一局），是已知长尾。想压时长，第一刀砍预备队数量（`reserve0`），
  别动 `TOUGH`——那是"让单位活过第一轮、双方缠在一起"的东西，是特意要的。

## 改之前先量

`tools/harness.js` 把 `<script>` 抽出来丢进 node 的 vm 跑，DOM 和 canvas 用 Proxy
顶掉。一局六百多秒的模拟本地几秒出结果，跑十局看分布是一条命令的事。

```bash
node -e "const {load}=require('./tools/harness');
const g=load('ww1.html'), upd=g.\$('update');
for(let i=0;i<20*700;i++){ upd(1/20); if(g.\$('over')) break; }
console.log(g.\$('units.length'), g.\$('reserve'));"
```

游戏里的 `UNITS`/`update` 都是 `const`，不会挂到 vm 的全局对象上，所以留了个
`g.$('表达式')` 探针在脚本作用域里求值。

**别猜数值。** 这个项目里几乎每次"感觉不对"的根因都不在感觉指的地方：压制显得弱
是因为伤亡把场上磨到只剩 14 个人、火力密不起来；缠斗短命是因为导弹没有最小射程；
飞机不肯飞攻击航线是因为三维距离导引会让它在目标正上方减速到悬停。都是靠埋点读
实测输出找出来的。

## 踩过的坑

- **别用关键词删行。** 曾经为了去掉屏幕震动，把所有含 "shake" 的行删掉，连
  `let over = false, reTimer = 0, tick = 0, shake = 0;` 整行一起删了，`tick`
  没了，四个文件同时崩。
- **一个数字别管两件事。** 这条坑踩过两回。第一回：装甲只有一级、飞机也算装甲单位，
  于是同一个 `vs[0]` 既决定"能不能打赢空战"又决定"能不能凿穿坦克"；加了 `vsH` 只
  把坦克拆出去，飞机和半履带车照旧共用一个数。第二回：`vs` 本身就是两件事挤在一起
  ——步枪的 `vs[1]=6.3` 看着像"步枪特别克步兵"，其实只是 `dmg:5` 太小要乘回来。
  现在拆成 `dmg × pow × ARM[dc][ac]`：`pow` 是威力基数，`ARM` 是纯克制表，`air`
  独立成列。加武器只标弹种，别再手调系数。
- **单位大小是手画的，会漂。** 校准之前四个版本的坦克全都比步兵矮。现在启动时
  `calibrate()` 自动量包围盒反算系数，改画法不用手动重算——但 `sizeM` 标的是
  "视觉占位高度"，骑兵举着长矛、火炮扬着炮管都算进块头里，不是物理尺寸。
- **奇观的触发阈值要照实测分布定。** 骑兵突击最早卡"敌方地面单位 ≤6"，结果场上
  常年三十来个单位，一局都轮不到一次。
